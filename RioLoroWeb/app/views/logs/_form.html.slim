= form_with(model: log, local: true) do |form|
  - if log.errors.any?
    #error_explanation
      h2
        = pluralize(log.errors.count, "error")
        |  prohibited this log from being saved:
      ul
        - log.errors.full_messages.each do |message|
          li
            = message
  .field
    = form.label :fecha
    = form.text_field :fecha, id: :log_fecha
  .field
    = form.label :hora
    = form.text_field :hora, id: :log_hora
  .field
    = form.label :accion
    = form.text_field :accion, id: :log_accion
  .actions
    = form.submit
