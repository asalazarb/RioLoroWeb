= form_with(model: tipo_usuario, local: true) do |form|
  - if tipo_usuario.errors.any?
    #error_explanation
      h2
        = pluralize(tipo_usuario.errors.count, "error")
        |  prohibited this tipo_usuario from being saved:
      ul
        - tipo_usuario.errors.full_messages.each do |message|
          li
            = message
  .field
    = form.label :nombreTipo
    = form.text_field :nombreTipo, id: :tipo_usuario_nombreTipo
  .field
    = form.label :nivelPermiso
    = form.text_field :nivelPermiso, id: :tipo_usuario_nivelPermiso
  .actions
    = form.submit
